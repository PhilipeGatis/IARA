# ハードウェアアーキテクチャ資料：水槽管理システム

## 📋 概要

本システムは**多層防御**トポロジーを採用し、入力側（AC）でのノイズフィルタリング、メインバス（DC）での安定化、エッジ側（アクチュエーター）での誘導性スパイク抑制を行います。

```
┌─────────────────────┐     ┌─────────────────────┐     ┌─────────────────────┐
│ レイヤー1: AC        │ ──► │ レイヤー2: DC        │ ──► │ レイヤー3: エッジ    │
│ 入力/保護           │     │ バス/ロジック       │     │ 周辺機器/抑制       │
│                     │     │                     │     │                     │
│ • ACヒューズ        │     │ • 12.53V電源        │     │ • フライバックD     │
│ • NTC（突入電流）   │     │ • LM2596 → 5.1V    │     │ • デカップリングC   │
│ • EMIフィルタ（Y）  │     │ • コンデンサバンク  │     │ • センサー（GX12）  │
│ • キャニスターリレー│     │ • 8ch MOSFET        │     │ • ポンプ + バルブ   │
└─────────────────────┘     └─────────────────────┘     └─────────────────────┘
```

---

## ⚡ レイヤー1：AC入力・保護（インフラストラクチャ）

電力入力アーキテクチャはシンプルさと最大限の電気的安全性を重視し、重いフィルタリング（EMIフィルターやNTC/突入電流制限など）は、スイッチング電源にすでに組み込まれているプロ用回路に委ねています。

### コンポーネント

| コンポーネント | 機能 |
|---|---|
| **ACガラス管ヒューズ**（3A〜5A） | 必須項目：短絡および火災リスクに対する保護 |
| **SSRリレーモジュール 1CH** | キャニスターフィルター独立制御（ESP32経由、AC） |

### 配線図

```text
[ IEC C14 入力 ]
      │
      ├─── [ 接地ピン (緑) ] ─────────────────┬───► [ 電源 接地 (G) 端子 ]
      │                                       │
      │                                       └───► [ キャニスター出力 接地ピン ]
      │         
      ├─── [ 中性ピン (青) ] ─────────────────┬───► [ 電源 中性 (N) 端子 ]
      │                                       │
      │                                       └───► [ キャニスター出力 中性ピン ]
      │
      └─── [ 相ピン (茶) ] ───── [ ヒューズ ] ──┐
                                                │
             ┌──────────────────────────────────┘
             │                                  
             ├───────────────────► [ 電源 相 (L) 端子 ]
             │
             └───► [ SSRリレー ネジ1 ] ── スイッチ ──► [ SSRリレー ネジ2 ] ──► [ キャニスター出力 相ピン ]
```

---

## 🔋 レイヤー2：DCバス・ロジック（分配）

電力をロジックレベルに変換し、重負荷スイッチング時のESP32安定性を維持します。

### コンポーネント

| コンポーネント | 機能 |
|---|---|
| **スイッチング電源 180W** | 12.53Vに調整 |
| **ヒューズ T5AL250V** | 8台のポンプとセンサーの物理的保護 |
| **LM2596** | 5.1Vに調整されたステップダウン（ESP32電源） |
| **1× 470µF 16V** | MOSFETの12V入力に並列（ポンプ起動時の電圧降下を防止） |
| **4× 1000µF 10V** | 5V出力に並列（ESP32のスパイクや変動対策、「無停電電源」として安定動作を保証） |

### 配線図

```text
[ 電源 12.53V ]
   │              │
 (V+)           (V─) ──────────────────────────────┐ (スターGND)
   │              │                                │
[T5Aヒューズ]     │                                │
   │              │                                │
   ├──────────────┼────────────────┐               │
   │              │                │               │
[LM2596 IN+]  [LM2596 IN─]    [MOSFET VIN]   [MOSFET GND]
   │              │                │               │
(出力 5.1V)       │         (1× 470µF 16V)         │
   │              │                │               │
(4× 1000µF 10V)   │                │               │
   │              │                │               │
[ESP32 VIN]   [ESP32 GND]          │               │
   │              │          [ 8ch MOSFET ]        │
   │              └────────────────┴───────────────┘
```

> [!IMPORTANT]
> **電源GND — 電源側でスター接続**：**電源系**の負側基準（MOSFETモジュール、LM2596、ポンプ、ソレノイド）は、グランドループを防ぐため電源のV−端子に直接戻してください。

> [!CAUTION]
> **信号GND — 逆のルール**：**センサー**（フロートスイッチ、超音波）のGNDは、**ESP32のGNDピン**に接続し、電源端子に直接戻しては**いけません**。
>
> ESP32は各入力を*自身の*GNDピンを基準に測定します。電源端子を基準にしたセンサーは、その2点間の電位差をそのままGPIOに与えます。ポンプ起動時にこの差は**−1 V**を超え、ESP32の絶対最小定格−0.3 Vを大きく下回るため、ダメージが蓄積してピンが破損します。
>
> GPIO5とGPIO19はまさにこの原因で焼損しました。[`PROTECAO_ELETRICA.md`](PROTECAO_ELETRICA.md) を参照。

---

## 🌊 レイヤー3：周辺機器・抑制（エッジ）

誘導性ノイズ（フライバック）の軽減および長距離ケーブル（1.2m）でのセンサー読み取り安定化を担当します。

### コンポーネント

| コンポーネント | 用途 | 数量 |
|---|---|---|
| **FR154ダイオード**（高速リカバリ） | 給排水ポンプ | 2 |
| **FR154ダイオード**（高速リカバリ） | ペリスタルティックポンプ（肥料4 + プライム1） | 5 |
| **1N5822ダイオード**（ショットキー） | ソレノイドバルブ（チャンネル8） | 1 |
| **10µF / 22µF 50Vコンデンサ** | ケーブル端でのセンサーデカップリング（GX12） | — |

> [!NOTE]
> **合計：8個のフライバックダイオード** — モーターチャンネルごとに1個（7× FR154 + 1× 1N5822）。すべてケーブル端のモーター近傍に設置。

### 配線図 — アクチュエーター

```
[ MOSFETチャンネル ] ──────── (1.2mワイヤ) ──────┬────────── (ポンプ +)
                                                 │
                                           [ フライバックD ]
                                           (ストライプは+側)
                                                 │
[ バスGND ] ─────────────── (1.2mワイヤ) ────────┴────────── (ポンプ ─)
```

> [!CAUTION]
> **フライバックダイオード**：必ず**ケーブル端**（モーター近傍）に設置してください。1.2mケーブルがアンテナのようにノイズを放射するのを防ぎます。

### 配線図 — 超音波センサー（A02YYUW、UART）

距離を**UART**で送信する防水超音波センサー。TRIG/ECHOは使わず、分圧回路も不要です。コマンドなしで自動送信し、約100 msごとに1フレームを出力します。

| 線 | 機能 | 接続先 | 備考 |
|---|---|---|---|
| **VCC**（赤） | 電源 | ESP32 3.3V | ⚠️ 5Vは不可 — 下記の警告参照 |
| **GND**（黒） | 帰線 | **ESP32のGNDピン** | ⚠️ 電源端子は不可 |
| **TX** | センサーのデータ出力 | GPIO34（RX2） | 10kΩのプルアップが必要 |
| **RX** | 制御入力 | 3.3V | ファームウェアでは未使用 — 下記の注記参照 |

> データ線2本の色はロットによって異なります。一貫しているのは赤と黒のみです。出力を特定するには、センサーに給電して測定してください。**TX**は約3.3Vで待機し、動きがあります。RXは変化しません。

```
   ESP32 3.3V ──────┬──────────────────►  VCC  （赤）
                    │
                [ 10 kΩ ]                        A02YYUW
                    │                            （防水）
   ESP32 GPIO34 ────┴──────────────────►  TX   （データ出力）
    (RX2)

   ESP32 3.3V ─────────────────────────►  RX   （制御、未使用）

   ESP32 GND ──────────────────────────►  GND  （黒）
    （基板のピン。電源端子は不可）
```

> [!WARNING]
> **3.3Vで給電してください。5Vは不可です。** A02YYUWは3.3〜5Vに対応しますが、出力の論理レベルはVCCに追従します。5VではTX線がGPIO34に5Vを与え、永久的に破損させます。3.3Vで動作させることこそが、分圧回路なしの直結を可能にしています。

> [!IMPORTANT]
> **GPIO34と3.3Vの間に10kΩのプルアップ。** GPIO34は入力専用で、**内部プルアップがありません**。外付け抵抗がないと、ケーブルが外れたりセンサーが無給電の際に線がフローティングとなり、ファームウェアがノイズをデータとして読み取ります。プルアップがあれば線はUARTのアイドル状態であるHighで待機し、データ欠落は `SafetyWatchdog::readUltrasonic()` の2秒タイムアウトで検出される明確な故障になります。

> [!NOTE]
> **制御線（センサーのRX）は未使用です。** ファームウェアは `Serial2` に書き込みを行わず、センサーは自律的に送信します。この線は連続送信モードを維持するため**3.3V**に接続してください。避けるべきはフローティングのまま放置することです。
>
> 現在は `PIN_US_TX = GPIO18`（`include/Config.h`）に接続されており、UARTのTXピンとしてHighで待機するため同じ効果になっています。3.3Vに直結すればGPIO18を他の用途に解放できます。

> [!TIP]
> **プロトコル：** 9600 baud、8N1。4バイトフレーム — `0xFF`、`DataH`、`DataL`、`Checksum`。距離は**ミリメートル**単位（`(DataH << 8) | DataL`）、チェックサムは `(0xFF + DataH + DataL) & 0xFF` です。ファームウェアはチェックサム不正のフレームを破棄し、5サンプルのメディアンフィルタを適用します。

> [!CAUTION]
> **ローカルデカップリング。** 3.3V動作では定格範囲の下限で動くため、ポンプ起動時の電圧降下に敏感になります。症状は、まさに排水と給水の最中に読み取り値が壊れることです。**100 µF + 100 nFをセンサーのコネクタ側**に取り付けてください（基板側ではありません）。[`PROTECAO_ELETRICA.md`](PROTECAO_ELETRICA.md) を参照。

### 配線図 — 最大水位インターロック（リードスイッチ）

最大水位の遮断は**ファームウェアを経由しません**。**GPIO33** とMOSFETモジュールの入力 **IN7**（給水ポンプのチャンネル）を結ぶ信号線に、リードスイッチを直列に挿入します。水が最大水位に達すると接点が開き、制御信号が断たれてポンプが停止します。ESP32がハングしていても、再起動中でも、超音波センサーが沈黙していても機能します。

構成：**NO**（ノーマルオープン）リードスイッチと、水面外のEVAフロートに保持された磁石。通常水位では磁石が近くにあり接点は閉。水位が上がるとフロートが磁石を遠ざけ、接点が開きます。

| 状況 | 磁石 | リード | 信号 | ポンプ |
|---|---|---|---|---|
| 通常水位 | 近い | 閉 | 通過 | 動作可能 |
| 最大水位 | 離れる | 開 | 遮断 | **停止** |
| 断線 | — | — | 遮断 | **停止** |

```
  ESP32 GPIO33 ───[ リード ]───┬─── IN7（MOSFETモジュール、CH7）
                               │
                           [ 1 kΩ ]
                               │
                           [ 100 nF ]
                               │
                        モジュールのGND
```

> [!CAUTION]
> **1 kΩの抵抗は必須で、モジュール側**（リードが線を切る点より先）に配置します。MOSFETの入力は蓄積電荷で動作するため、線が開いた状態でこの抵抗がないとゲートがフローティングとなり、電荷を保持してMOSFETが部分導通したままになる可能性があります。つまりポンプが停止しません。
>
> 製作前に、IN7とモジュールのGND間の抵抗を測定してください。すでに10 kΩ〜100 kΩを示す場合、モジュールに内蔵プルダウンがあり外付け抵抗は不要です。

> [!WARNING]
> **リードスイッチをポンプの電源に直列に入れないでください。** リード接点の定格は0.5〜1 Aですが、給水ポンプは起動時に数アンペアを引きます。接点がアークを起こし、最終的に溶着します。インターロックを無効化する、気づけない故障です。信号線上のこの位置なら、流れる電流は4 mA未満です。

> [!TIP]
> **組み立てを封止する前に、解放距離を確認してください。** リードにはヒステリシスがあり、閉じる距離より開く距離のほうが大きくなります。マルチメータの導通モードで磁石を接点が開くまで遠ざけ、その距離を記録します。フロートのストロークはその2〜3倍必要です。また、ストロークの途中のどの位置でも再び閉じないことを確認してください。

> [!IMPORTANT]
> **ファームウェアはこの遮断を検知できません。** リードが動作すると、ステートマシンはタイムアウトするまで `REFILLING` に留まり、`ERROR` で終了します。通常運転では超音波センサーが先にセットポイントに到達するため、これは起こりません。リードは安全網であり、通常の停止手段ではありません。
>
> 以前XKC-Y25静電容量センサー用だった **GPIO4** は空いています（このセンサーは一度も設置されず、プロジェクトから削除されました）。2つ目のリードをそこに接続すればファームウェアに可視性が戻りますが、コード変更が必要です。

### 配線図 — TFT ST7735ディスプレイ (SPI)

1.8インチカラーディスプレイ、128×160ピクセル。**3.3V**で動作 — 5Vは非互換。カスタムピンでソフトウェアSPI（ビットバンギング）を使用します。

| ディスプレイのピン | ESP32接続 | GPIO | 備考 |
|---|---|---|---|
| **VCC** | 3.3V | — | ⚠️ 絶対に5Vを使用しない |
| **GND** | GND | — | |
| **CS** | D15 | GPIO15 | チップセレクト (Chip Select) |
| **RESET** | ENピン | — | ESP32と共有のハードウェアリセット |
| **A0 (DC)** | TX2 | GPIO17 | データ/コマンド (Data/Command) |
| **SDA (MOSI)** | D23 | GPIO23 | SPIデータ |
| **SCK** | RX2 | GPIO16 | SPIクロック |
| **LED** | 3.3V | — | 固定バックライト (空きGPIOなし) |

```
ESP32 3.3V  ────►  VCC
ESP32 GND   ────►  GND
ESP32 D15   ────►  CS
ESP32 EN    ────►  RESET
ESP32 TX2   ────►  A0 (DC)
ESP32 D23   ────►  SDA (MOSI)
ESP32 RX2   ────►  SCK
ESP32 3.3V  ────►  LED (固定バックライト)
```

> [!WARNING]
> ディスプレイの**RESET**ピンはESP32の**EN**ピンに接続してください（GPIOではありません）。これにより、ディスプレイがマイクロコントローラと一緒にリセットされます。ENがHIGH（通常動作）のとき、ディスプレイは正常に動作します。

> [!NOTE]
> ESP32 DevKit V1ブレイクアウトボードでは、**D16**および**D17**はそれぞれ**RX2**および**TX2**とラベル付けされています。端子台のラベルを使用して正しい位置を確認してください。

---

### 配線図 — リザーバーフロートスイッチ

リザーバー満水を検出する水平フロートスイッチ。TPAの `FILLING_RESERVOIR` 状態でソレノイドを閉じます。ファームウェアは信号を受け入れる前に「満水」を5回連続で読み取ることを要求します（デバウンス約250ms）。

| ピン | 接続 | GPIO | 設定 |
|---|---|---|---|
| **端子 1** | GPIO19 | GPIO19 | `INPUT_PULLUP` — LOWでアクティブ |
| **端子 2** | **ESP32のGNDピン** | — | ⚠️ 電源端子には絶対に接続しない |

```
ESP32 GPIO19 ─────── [ フロート ] ─────── ESP32 GND（基板のピン）
         INPUT_PULLUP, LOWでアクティブ (満水 = LOW)
         2本の線は同じケーブル内を最後まで一緒に配線する
```

> [!NOTE]
> フロートスイッチは当初GPIO5を使用していましたが、このピンはESP32のストラッピングピンであり干渉が発生していました（静止時約2.5V）。以前はパネルのナビゲーションボタンが使用していたGPIO19に移動しました。

> [!IMPORTANT]
> GPIO19がフロートスイッチ用になったため、**パネルのナビゲーションボタンは無効化されています**（`include/Config.h` の `PIN_BTN = 255`）。ディスプレイは自動でページを切り替えます。メニュー（TPA開始／メンテナンスモード）はWebダッシュボードからのみアクセス可能です。ボタンを復活させるには、空いているGPIOを選んで `PIN_BTN` を更新してください。

---

## 🛠️ 安全な実装上の注意点

1. **AC 接続** — 110V/220Vのすべてのハンダ付け接点および接続部は、最大限の安全性を確保するために熱収縮チューブで絶縁してください。

2. **コンデンサの極性** — すべての電解コンデンサの負極ストライプを確認してください（特に5.1Vで動作する1000µF 10V）。

3. **フライバックダイオード** — 必ずケーブル端（モーター近傍）に設置し、1.2mケーブルがアンテナのようにノイズを放射するのを防いでください。

4. **スターGND** — すべての負側基準は電源のV−端子に直接戻し、グランドループを防止してください。

5. **サイフォン現象（水力学）** — 排水ポンプ停止後に重力で水槽が排水され続けるのを防ぐため、排水ポンプと並列にソレノイドバルブを配線する（同じMOSFETチャンネルに接続し、それぞれにフライバックダイオードを取り付ける）か、ホースに空気穴を開けてください。
