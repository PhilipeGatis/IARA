; Copy this file to `secrets.ini` and fill in your own network.
; `secrets.ini` is gitignored so credentials never reach the repository.
[secrets]
build_flags =
    -D WIFI_SSID='"YOUR_WIFI_SSID"'
    -D WIFI_PASSWORD='"YOUR_WIFI_PASSWORD"'
